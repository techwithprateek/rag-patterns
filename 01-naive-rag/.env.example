# Choose your LLM provider: openai | groq | ollama
LLM_PROVIDER=openai

# OpenAI
OPENAI_API_KEY=sk-...

# Groq (if LLM_PROVIDER=groq) — free tier available at console.groq.com
GROQ_API_KEY=gsk_...

# Ollama (if LLM_PROVIDER=ollama) — run `ollama serve` locally first
# OLLAMA_MODEL=llama3.2
